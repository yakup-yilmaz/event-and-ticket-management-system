package com.example.ticketsystem.entity;

/**
 * Bildirim türü — istemci filtreleme / ikon için.
 */
public enum NotificationType {
    PRICE_CHANGED,
    DATE_CHANGED,
    STATUS_CHANGED,
    EVENT_CANCELLED,
    STOCK_AVAILABLE,
    EVENT_DELETED
}
